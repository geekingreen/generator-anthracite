<div class="row">
    <div class="<% if (twbs) { %>col-lg-12<% } else { %>large-12 columns<% } %>">
        <ul class="<% if (twbs) { %>list-inline<% } else { %>inline-list<% } %>">
            <li>{{#linkTo 'about'}}About{{/linkTo}}</li>
            {{#each controller}}
                {{partial myPartial}}
            {{/each}}
        </ul>
    </div>
</div>
